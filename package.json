{
  "name": "polar",
  "version": "0.1.0",
  "private": true,
  "description": "Polar runtime bootstrap workspace",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "check:boundaries": "node scripts/check-pi-mono-imports.mjs",
    "test": "node --test tests/**/*.test.mjs",
    "check": "npm run check:boundaries && npm test"
  },
  "overrides": {
    "rimraf": "npm:rimraf@6.1.2",
    "gaxios": {
      "rimraf": "npm:rimraf@6.1.2"
    }
  }
}
